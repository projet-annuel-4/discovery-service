spring:
  cloud:
    kubernetes:
      config:
        enabled: true
        sources:
          - namespace: default
            name: discovery-service
      reload:
        enabled: true
        mode: event
        strategy: polling